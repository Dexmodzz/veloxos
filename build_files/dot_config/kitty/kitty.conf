# VeloxOS kitty config
hide_window_decorations yes

# Tema dinamico di DankMaterialShell (matugen).
# DMS rigenera questi file in ~/.config/kitty/ a ogni cambio tema/wallpaper.
include dank-tabs.conf
include dank-theme.conf
