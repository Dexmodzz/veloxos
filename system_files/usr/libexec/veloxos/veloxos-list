#!/usr/bin/env bash
set -euo pipefail

PACKAGES_LIST="/var/lib/veloxos/packages.list"

if [[ ! -f "$PACKAGES_LIST" ]] || [[ ! -s "$PACKAGES_LIST" ]]; then
    echo "No packages installed via VeloxOS overlay."
    exit 0
fi

echo "VeloxOS overlay packages:"
echo ""
while IFS= read -r pkg; do
    [[ -z "$pkg" ]] && continue
    echo "  • $pkg"
done < "$PACKAGES_LIST"
